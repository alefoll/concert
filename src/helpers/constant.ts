import { resolve } from "path";

export const ALLOWED_EXTENSIONS = [".mkv"];
export const FFMETADATA_EXTENSIONS = ".ffmetadata";

export const CONFIG_PATH = resolve("./config.json");
export const DATA_FOLDER = resolve("./data");

